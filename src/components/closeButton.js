import React from 'react';

const closeButton = (props) => {


    return (

        <button className="btn red roundButtons" style={{ position: 'absolute' }}>
            X
        </button>
    );
}

export default closeButton;